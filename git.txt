git init

git status

git add --all  # add all the folder


git commit -m "First git"  # which name do you want to use


git status  # to see what is the status


git log  # to see all the history

git status

git commit -m "add a new file"


git reset --hard "commit id " # if resst before git


git reflog # to see all commit history

git rm 'file name' 
    git commit -m 'make a note' # to remove any file , do not use colon 


git branch --list  # to see all branch


git branch first/add-something  # to add new branch


git switch first/add-something   # to go the branch

    now i work in a branch 

git switch second update  # to switch other branch

git reflog  # to see all 

git branch -m feutre/add-something   # to rename the branch 


git remote add origin git@github.com:mahfuzurrahman8747/Research-and-Blog-writing-using-Crewai.git    # to add github

git push -u origin main 

git clone 'git link' # to clone github code to your local, donot use colon